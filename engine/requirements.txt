# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Database
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
alembic==1.13.1

# Redis
redis[hiredis]==5.0.4

# Configuration & validation
pydantic==2.7.1
pydantic-settings==2.3.1

# Korean broker APIs
pykrx                      # 한국거래소 공공 데이터 (인증 불필요, 개발/데모용)
# python-kis               # 한국투자증권 Open API (PyPI 미등록 — KIS 사용 시 수동 설치)
# mojito2==0.1.11          # alternative broker library

# Trading / Technical analysis
pandas==2.2.2
numpy==1.26.4
# pandas-ta==0.3.14b0  # pandas 2.x 호환 문제로 제거 → researcher.py 내 순수 pandas 구현으로 대체

# Scheduling
apscheduler==3.10.4

# HTTP client / HTML 파싱
httpx==0.27.0
beautifulsoup4==4.12.3

# Machine learning (optional - ML strategy)
scikit-learn==1.5.0
# torch==2.3.0             # uncomment for LSTM strategies

# Utilities
python-dotenv==1.0.1
structlog==24.2.0
